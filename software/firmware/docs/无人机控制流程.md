# 无人机控制流程

## stabilizerTask 主循环流程图

```mermaid
flowchart TD
    subgraph 初始化阶段
        A[stabilizerTask 启动] --> B[systemWaitStart<br/>等待系统启动]
        B --> C[等待传感器校准<br/>sensorsAreCalibrated]
        C --> D[初始化 rateSupervisor<br/>频率监控器]
        D --> E[打印 Ready to fly]
    end

    subgraph 主循环["主循环 (1000Hz)"]
        E --> F[sensorsWaitDataReady<br/>等待传感器中断]
        
        F --> G{startPropTest?}
        G -->|是| H[testState = configureAcc<br/>启动螺旋桨测试]
        H --> I
        G -->|否| I{testState != testDone?}
        
        I -->|是 - 测试模式| J[sensorsAcquire<br/>采集传感器数据]
        J --> K[testProps<br/>运行电机测试]
        K --> W
        
        I -->|否 - 正常飞行| L{估计器类型变化?}
        L -->|是| M[stateEstimatorSwitchTo<br/>切换估计器]
        L -->|否| N{控制器类型变化?}
        M --> N
        
        N -->|是| O[controllerInit<br/>切换控制器]
        N -->|否| P
        O --> P
        
        subgraph 核心控制流程
            P[stateEstimator<br/>状态估计器更新] --> Q[compressState<br/>压缩状态数据]
            Q --> R[commanderGetSetpoint<br/>获取目标设定点]
            R --> S[compressSetpoint<br/>压缩设定点]
            S --> T[sitAwUpdateSetpoint<br/>情景感知更新]
            T --> U[controller<br/>控制器计算输出]
        end
        
        U --> V[checkEmergencyStopTimeout<br/>检查紧急停止超时]
        V --> V1{emergencyStop 或<br/>未解锁?}
        V1 -->|是| V2[powerStop<br/>停止电机]
        V1 -->|否| V3[powerDistribution<br/>功率分配到电机]
        V2 --> W
        V3 --> W
        
        W[calcSensorToOutputLatency<br/>计算延迟] --> X[tick++]
        X --> Y[STATS_CNT_RATE_EVENT<br/>统计频率]
        Y --> Z{rateSupervisorValidate?}
        Z -->|频率异常| Z1[打印警告]
        Z -->|正常| F
        Z1 --> F
    end

    style A fill:#e1f5fe
    style F fill:#fff3e0
    style P fill:#e8f5e9
    style U fill:#e8f5e9
    style V3 fill:#ffebee
    style V2 fill:#ffcdd2
```

## 数据流向图

```mermaid
flowchart LR
    subgraph 传感器层
        IMU[BMI088<br/>加速度计+陀螺仪]
        BARO[MS5611<br/>气压计]
        TOF[<br/>激光测距]
        FLOW[<br/>光流]
    end

    subgraph 状态估计
        SE[stateEstimator]
        KF[kalmanTask<br/>500Hz 预测]
    end

    subgraph 控制层
        CMD[commander<br/>指令获取]
        SITAW[sitAw<br/>情景感知]
        CTRL[controller<br/>PID控制]
        PWR[powerDistribution<br/>混控分配]
    end

    subgraph 执行层
        M1[电机 M1]
        M2[电机 M2]
        M3[电机 M3]
        M4[电机 M4]
    end

    IMU -->|1000Hz| SE
    BARO -->|50Hz| SE
    TOF -->|异步| KF
    FLOW -->|异步| KF
    
    SE <-->|状态交换| KF
    SE -->|state| CMD
    CMD -->|setpoint| SITAW
    SITAW -->|修正setpoint| CTRL
    SE -->|state| CTRL
    
    CTRL -->|control| PWR
    PWR --> M1
    PWR --> M2
    PWR --> M3
    PWR --> M4
```

## 频率层次图

```mermaid
flowchart TB
    subgraph 1000Hz["1000 Hz 层"]
        S1[sensorsWaitDataReady]
        S2[stateEstimator 接口调用]
        S3[controller 控制器]
        S4[powerDistribution 电机输出]
    end

    subgraph 500Hz["500 Hz 层"]
        K1[kalmanCorePredict<br/>卡尔曼预测]
        K2[kalmanCoreFinalize<br/>状态最终化]
    end

    subgraph 定时更新["定时更新层"]
        B1[气压计融合（启用时 50Hz）]
        A1[加速度姿态校正（25Hz）]
    end

    subgraph 异步["异步事件驱动"]
        E1[光流测量更新]
        E2[ToF距离更新]
        E3[外部位置更新]
    end

    S1 --> S2 --> S3 --> S4
    S2 -.->|唤醒| K1
    K1 --> K2
    K2 -.->|状态复制| S2
    B1 -.->|融合| K1
    A1 -.->|融合| K1
    E1 -.->|队列| K1
    E2 -.->|队列| K1
    E3 -.->|队列| K1
```

## 关键变量说明

| 变量            | 类型           | 说明                                |
| --------------- | -------------- | ----------------------------------- |
| `sensorData`    | `sensorData_t` | 传感器原始数据（IMU/气压计/磁力计） |
| `state`         | `state_t`      | 当前状态估计（姿态/位置/速度）      |
| `setpoint`      | `setpoint_t`   | 目标设定点（来自遥控/上位机）       |
| `control`       | `control_t`    | 控制器输出（roll/pitch/yaw/thrust） |
| `tick`          | `uint32_t`     | 循环计数器                          |
| `emergencyStop` | `bool`         | 紧急停止标志                        |
| `testState`     | `TestState`    | 螺旋桨测试状态机                    |

---

## 状态估计系统详解

系统支持两种状态估计器，通过函数指针表实现运行时切换：

### 估计器选择架构

```mermaid
flowchart TD
    subgraph EstimatorInterface["estimator.c - 统一接口层"]
        A[stateEstimator] --> B{currentEstimator}
        B -->|complementaryEstimator| C[estimatorComplementary]
        B -->|kalmanEstimator| D[estimatorKalman]
    end
    
    subgraph Complementary["互补滤波 - 无独立任务"]
        C --> C1[sensfusion6UpdateQ<br/>四元数更新 @250Hz]
        C1 --> C2[positionEstimate<br/>位置估计 @100Hz]
    end
    
    subgraph Kalman["卡尔曼滤波 - 独立任务"]
        D --> D1[累积IMU数据]
        D1 --> D2[唤醒 kalmanTask]
        D2 --> D3[kalmanTask<br/>独立FreeRTOS任务]
    end
```

### 估计器对比

| 特性           | 互补滤波         | 卡尔曼滤波                |
| -------------- | ---------------- | ------------------------- |
| **独立任务**   | ❌ 无（函数调用） | ✅ 有（kalmanTask）        |
| **状态维度**   | 3维（仅姿态）    | 9维（位置+速度+姿态误差） |
| **传感器融合** | 简单加权混合     | 协方差最优估计            |
| **外部传感器** | 仅TOF            | TOF/光流/UWB/外部位置等   |
| **CPU开销**    | 低               | 较高                      |
| **适用场景**   | 基础姿态稳定     | 定点悬停/自主飞行         |
| **默认选择**   | 作为备选           | ✅ 当前系统默认            |

---

## 卡尔曼滤波器详解

### 双任务协作架构

卡尔曼滤波采用**生产者-消费者**模式，分离高频数据采集与计算密集型滤波：

```mermaid
flowchart TB
    subgraph StabilizerLoop["Stabilizer 主循环 @1000Hz"]
        A[传感器数据就绪] --> B["estimatorKalman()"]
        B --> C["累积 acc/gyro 到累加器"]
        C --> D["累积 thrust 推力命令"]
        D --> E["复制 taskEstimatorState → state"]
        E --> F["xSemaphoreGive(runTaskSemaphore)"]
    end

    subgraph KalmanTask["kalmanTask 独立任务"]
        G["xSemaphoreTake(runTaskSemaphore)"] --> H{"resetEstimation?"}
        H -->|是| H1[estimatorKalmanInit 重置]
        H -->|否| I
        H1 --> I
        
        I{"osTick >= nextPrediction?"}
        I -->|是 @500Hz| J[predictStateForward]
        I -->|否| K[跳过预测]
        
        J --> J1["计算 gyro/acc 平均值"]
        J1 --> J2["kalmanCorePredict 状态预测"]
        J2 --> J3["清零累加器"]
        J3 --> L
        K --> L
        
        L[kalmanCoreAddProcessNoise] --> M{"useBaroUpdate?"}
        M -->|是 @50Hz| M1[kalmanCoreUpdateWithBaro]
        M -->|否| N
        M1 --> N

        N --> N0[updateAccelAttitudeIfReady @25Hz]
        N0 --> N1
        
        N1[updateQueuedMeasurements] --> N2{"有TOF?"}
        N2 -->|是| N3[kalmanCoreUpdateWithTof]
        N2 -->|否| N4{"有速度量测?"}
        N3 --> N4
        N4 -->|是| N5[kalmanCoreUpdateWithVelocity]
        N4 -->|否| N6{"有位置?"}
        N5 --> N6
        N6 -->|是| N7[kalmanCoreUpdateWithPosition]
        N6 -->|否| O
        N7 --> O
        
        O{"doneUpdate?"}
        O -->|是| P[kalmanCoreFinalize]
        O -->|否| Q
        P --> P1["姿态误差归入四元数"]
        P1 --> P2["更新旋转矩阵 R"]
        P2 --> P3["协方差边界检查"]
        P3 --> Q
        
        Q[kalmanCoreExternalizeState] --> R["导出到 taskEstimatorState"]
        R --> G
    end

    F -.->|信号量| G
    R -.->|下次循环读取| E

    style B fill:#e3f2fd
    style J fill:#e8f5e9
    style N fill:#fff3e0
    style P fill:#fce4ec
```

### 数据同步机制

```mermaid
flowchart LR
    subgraph 共享数据["共享数据区 (dataMutex 保护)"]
        ACC[accAccumulator<br/>加速度累加器]
        GYRO[gyroAccumulator<br/>角速度累加器]
        THR[thrustAccumulator<br/>推力累加器]
        SNAP_G[gyroSnapshot<br/>陀螺仪快照]
        SNAP_A[accSnapshot<br/>加速度快照]
        STATE[taskEstimatorState<br/>输出状态]
    end

    subgraph Stabilizer["Stabilizer @1kHz"]
        S1[sensorsReadAcc] -->|累加| ACC
        S2[sensorsReadGyro] -->|累加| GYRO
        S3[control.thrust] -->|累加| THR
        S1 -->|复制| SNAP_A
        S2 -->|复制| SNAP_G
        STATE -->|复制到 state| S4[返回给控制器]
    end

    subgraph KalmanTask["kalmanTask @500Hz 预测"]
        K1[读取并清零累加器] --> ACC
        K1 --> GYRO
        K1 --> THR
        K2[读取快照] --> SNAP_G
        K2 --> SNAP_A
        K3[写入估计结果] --> STATE
    end
```

### 卡尔曼状态向量

滤波器估计9维状态向量：

```
状态向量 S[9]:
┌─────────────────────────────────────────────────────────┐
│ S[0] = KC_STATE_X   │ X位置 (世界坐标系, 米)            │
│ S[1] = KC_STATE_Y   │ Y位置 (世界坐标系, 米)            │
│ S[2] = KC_STATE_Z   │ Z位置 (世界坐标系, 米)            │
├─────────────────────────────────────────────────────────┤
│ S[3] = KC_STATE_PX  │ X速度 (机体坐标系, m/s)           │
│ S[4] = KC_STATE_PY  │ Y速度 (机体坐标系, m/s)           │
│ S[5] = KC_STATE_PZ  │ Z速度 (机体坐标系, m/s)           │
├─────────────────────────────────────────────────────────┤
│ S[6] = KC_STATE_D0  │ 姿态误差分量0 (误差四元数)        │
│ S[7] = KC_STATE_D1  │ 姿态误差分量1 (误差四元数)        │
│ S[8] = KC_STATE_D2  │ 姿态误差分量2 (误差四元数)        │
└─────────────────────────────────────────────────────────┘

协方差矩阵 P[9][9]: 状态不确定性估计
四元数 q[4]: 当前姿态四元数
旋转矩阵 R[3][3]: 机体到世界坐标系变换
```

### 测量更新队列系统

外部传感器通过队列异步送入滤波器：

```mermaid
flowchart TB
    subgraph 外部传感器["外部传感器 (异步产生数据)"]
        TOF[VL53L1 激光测距]
        FLOW[MTF01 速度量测]
        UWB[UWB 超宽带]
        EXT[外部定位系统]
    end

    subgraph 队列层["FreeRTOS 队列 (深度=10)"]
        Q1[tofDataQueue]
        Q2[velocityDataQueue]
        Q3[tdoaDataQueue]
        Q4[posDataQueue]
        Q5[distDataQueue]
        Q6[heightDataQueue]
    end

    subgraph KalmanUpdate["kalmanTask 测量更新"]
        U1[kalmanCoreUpdateWithTof<br/>高度校正]
        U2[kalmanCoreUpdateWithVelocity<br/>机体系速度校正]
        U3[kalmanCoreUpdateWithTDOA<br/>UWB定位校正]
        U4[kalmanCoreUpdateWithPosition<br/>位置直接校正]
    end

    TOF -->|estimatorEnqueueTOF| Q1
    FLOW -->|estimatorEnqueueVelocity| Q2
    UWB -->|estimatorEnqueueTDOA| Q3
    EXT -->|estimatorEnqueuePosition| Q4

    Q1 -->|while循环消费| U1
    Q2 -->|while循环消费| U2
    Q3 -->|while循环消费| U3
    Q4 -->|while循环消费| U4
```

### 预测步骤详解 (predictStateForward)

```c
// 核心逻辑伪代码
bool predictStateForward(uint32_t osTick, float dt) {
    // 1. 检查数据有效性
    if (gyroAccumulatorCount == 0 || accAccumulatorCount == 0)
        return false;
    
    // 2. 计算累积期间的平均值
    gyroAverage = gyroAccumulator / gyroAccumulatorCount * DEG_TO_RAD;  // deg/s → rad/s
    accAverage = accAccumulator / accAccumulatorCount * GRAVITY;        // G → m/s²
    thrustAverage = thrustAccumulator / thrustAccumulatorCount;
    
    // 3. 清零累加器，准备下一轮累积
    accAccumulator = {0};  accAccumulatorCount = 0;
    gyroAccumulator = {0}; gyroAccumulatorCount = 0;
    
    // 4. 判断飞行状态 (影响过程噪声)
    if (thrustAverage > IN_FLIGHT_THRUST_THRESHOLD)
        quadIsFlying = true;
    
    // 5. 执行卡尔曼预测
    kalmanCorePredict(&coreData, thrustAverage, &accAverage, &gyroAverage, dt, quadIsFlying);
    
    return true;
}
```

### 频率分层总结

| 层级         | 频率   | 操作                           | 说明                   |
| ------------ | ------ | ------------------------------ | ---------------------- |
| **IMU采样**  | 1000Hz | `estimatorKalman()`            | 累积acc/gyro，复制状态 |
| **状态预测** | 500Hz  | `predictStateForward()`        | 运动学前向传播         |
| **测量更新** | 按需   | `updateQueuedMeasurments()`    | 消费队列中的外部测量   |
| **气压计**   | 50Hz   | `kalmanCoreUpdateWithBaro()`   | 高度辅助（可选）       |
| **状态导出** | 每轮   | `kalmanCoreExternalizeState()` | 写入共享状态           |

### 关键常量定义

| 常量                         | 值    | 说明                           |
| ---------------------------- | ----- | ------------------------------ |
| `PREDICT_RATE`               | 500Hz | 卡尔曼预测频率                 |
| `BARO_RATE`                  | 50Hz  | 气压计更新频率（启用时）       |
| `ACCEL_ATTITUDE_RATE`        | 25Hz  | 加速度姿态校正频率             |
| `IN_FLIGHT_THRUST_THRESHOLD` | 0.1g  | 判定起飞的推力阈值             |
| `IN_FLIGHT_TIME_THRESHOLD`   | 500ms | 推力低于阈值后仍视为飞行的时间 |
| `MAX_COVARIANCE`             | 100   | 协方差上限                     |
| `MIN_COVARIANCE`             | 1e-6  | 协方差下限                     |

---

## 混控流程图 (powerDistribution)

